<h1>Show each product</h1>

<table class="table table-bordered">
  <tr>
    <th>Title</th>
    <th>Price</th>
    <th>Description</th>
  </tr>

    <tr>
      <td><%= @product.name %></td>
      <td><%= @product.price %></td>
      <td><%= @product.description %></td>
    </tr>
</table>

<%= link_to 'All products', products_path, class: 'btn btn-primary' %>
<%= link_to 'Add new product', new_product_path, class: 'btn btn-primary' %>
